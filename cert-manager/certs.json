[
  {
	"request": {
	  "key": {
		"algo": "${key_algo}",
		"size": ${key_size}
	  },
	  "hosts": [
		"*.builders.resin.io",
		"*.${DNS_TLD}",
		"*.${TLD}"
	  ],
	  "names": [
		{
		  "C": "${country}",
		  "L": "${locality_name}",
		  "O": "${org}",
		  "OU": "${org_unit}",
		  "ST": "${state}"
		}
	  ],
	  "CN": "${TLD}"
	}
  }
]
